<nav class="navbar navbar-expand-lg ">
  <div class="container-fluid">
    <a class="navbar-brand" ></a>
    <div class="dropdown">
      <a class="nav-link dropdown-toggle d-flex align-items-center gap-2" 
      href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        <%= render "shared/users/user_icon_attached", user: user %>
        <span class="header-username"><%= user.name %></span>
      </a>
      <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
        <!-- プルダウンメニューの内容 -->
        <li><%= link_to 'マイページ', user_path(current_user), data: { turbolinks: false }, class: 'dropdown-item' %></li>
        <% if user == current_user && current_user.email != User::GUEST_USER_EMAIL %>
          <li><hr class="dropdown-divider"></li>
          <li><%= link_to '設定', edit_user_registration_path, data: { turbolinks: false }, class: 'dropdown-item' %></li>
        <% end %>
        <li><hr class="dropdown-divider"></li>
        <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, data: { turbolinks: false },  class: 'dropdown-item' %></li>
      </ul>
    </div>
  </div>
</nav>
