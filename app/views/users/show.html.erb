<% content_for :title, @user.name %>

<div class="container mypage">
  <%= render 'shared/mypage_common', user: @user%>
  <%= render 'shared/list_of_posts', title: "#{@user.name}の投稿", posts: @posts%>
</div>
