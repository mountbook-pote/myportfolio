<% content_for :title, @user.name %>

<div class="container">
  <%= render 'shared/mypage_common', user: @user%>
  <%= render 'shared/list_of_posts', title: "#{@user.name}のいいね", posts: @favorite_posts %>
</div>
