<div class="both-column-images">
  <% images.each do |information| %>
    <div>
      <%= image_tag information[:image], class: "both-column-images__image" %>
      <p class="both-column-images__title brand-font"><%= information[:title] %></p>
    </div>
  <% end %>
</div>
