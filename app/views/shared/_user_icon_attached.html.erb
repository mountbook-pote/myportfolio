<% width = local_assigns[:size] || 40 %>

<% if user.image.attached? %>
  <%= image_tag user.image, class:"rounded-circle", width: width, height: width %>
  <% else %>
    <%= image_tag "default_avatar.png", class: "rounded-circle", size: "#{width}x#{width}" %>
<% end %>
