<% content_for :title, @department_name %>

<h2><%= @department_name %></h2>

<div class="artwork-grid">
  <% @met_works.each do |work| %>
    <div class="artwork-card">
      <%= link_to work.primary_image_small, target: "_blank", rel: "noopener" do %>
        <img src="<%= work.primary_image_small %>" alt="<%= work.title %>" width="300">
      <% end %>
      <h4><%= work.title %></h4>
      <% if @department_name == "All Departments" %>
        <p>ジャンル: <%= work.department %></p>
      <% end %>
      <p>製作者: <%= work.artist_display_name.presence || '不明' %></p>
      <p>制作年: <%= work.object_date || '不明' %></p>
      <p><%= link_to 'メトロポリタン美術館で見る', work.object_url, target: "_blank", rel: "noopener", class: "post-button" %></p>
      <% if current_user %>
        <p><%= link_to 'この作品を投稿', new_post_path(met_object_id: work.id), class: "post-button" %></p>
      <% else %>
        <p><%= link_to 'この作品を投稿', new_user_session_path(alert: 'アカウント登録もしくはログインしてください') %></p>
      <% end %>
    </div>
  <% end %>
</div>
<div><%= link_to 'もっと見る',request.path, class: "btn btn-light", style:"color: black" %></div>
